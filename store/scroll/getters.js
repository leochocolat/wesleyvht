const getters = {
    isLocked(state) {
        return state.isLocked;
    },

    lockPosition(state) {
        return state.lockPosition;
    },

    position(state) {
        return state.position;
    },
};

export default getters;
